# Data folder

This folder is intentionally left as a project placeholder for downloaded or local dataset copies. The project loads the IMDb genre dataset automatically using KaggleHub at runtime, so no hard-coded dataset path is required.
